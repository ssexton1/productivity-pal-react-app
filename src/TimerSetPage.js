
function TimerSetPage( {clock, start, stop, reset} ) {

    const handleClick = () => {
        
    }

    return(
        <div>
            {clock}
            {start}
            {stop}
            {reset}
        </div>
    )
}

export default TimerSetPage;